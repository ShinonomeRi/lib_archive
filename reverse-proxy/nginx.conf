server {
    listen 80;

    # Отдаём frontend-контейнер
    location / {
        proxy_pass http://frontend/;
    }

    # Прокси на backend API
    location /api/ {
        proxy_pass http://backend:8000/;
    }
}
